import { DatepipeExmPipe } from './datepipe-exm.pipe';

describe('DatepipeExmPipe', () => {
  it('create an instance', () => {
    const pipe = new DatepipeExmPipe();
    expect(pipe).toBeTruthy();
  });
});
